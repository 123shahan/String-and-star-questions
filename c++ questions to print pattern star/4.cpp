// strings questions 
// In c++ string is a sequence of characters.

#include<iostream>
using namespace std;
int main(){

    string str = "hello"; // initialize the string

    // find the length of the string in c++

    int len = str.length();// we can find the length of the string using length() and size().
    int len1 = str.size();
    cout<<"The length of the string is " << len <<endl;
    cout<<"The size of the string is " << len1 <<endl;






    return 0;
}